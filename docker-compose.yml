services:
  gartenplaner:
    image: gartenplaner:latest
    build: .
    ports:
      - "8080:3000"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    environment:
      - PORT=3000
      - DATA_FILE=/app/data/gartenplaner.json
